on:
  workflow_dispatch:
    inputs:
      enviornment:
        description: "Choose enviroment"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production

jobs:
  deploy: 
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Application
        run: echo "Depolying the application"
